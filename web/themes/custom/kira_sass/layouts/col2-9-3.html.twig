{#
/**
 * @file
 * Default theme implementation for a media layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 *
 * KM modification -- kicks in at md screen size rather than small
 *
 */
#}
{% if content %}
  <div{{ attributes.addClass('row') }}>

    {% if content.top %}
      <div {{ region_attributes.top.addClass('region--top') }}>
        {{ content.top }}
      </div>
    {% endif %}

    <div{{ region_attributes.first.addClass('col-md-9') }}>
      {{ content.first }}
    </div>

    <div{{ region_attributes.second.addClass('col-md-3') }}>
      {{ content.second }}
    </div>

    {% if content.bottom %}
      <div {{ region_attributes.bottom.addClass('region--bottom') }}>
        {{ content.bottom }}
      </div>
    {% endif %}

  </div>
{% endif %}
